package com.example.pos.nav.ui.theme

import androidx.compose.ui.graphics.Color

// New palette from user
val Midnight = Color(0xFF0F172A) // background (dark)
val Steel = Color(0xFF475569) // primary
val Slate = Color(0xFF94A3B8) // secondary
val Mist = Color(0xFFF1F5F9) // tertiary / light surface
val PureWhite = Color(0xFFFFFFFF) // background (light)

val TextOnDark = Color(0xFFFFFFFF)

// Backwards-compatible aliases
val Primary = Steel
val Secondary = Slate
val Tertiary = Mist
val BackgroundLight = PureWhite
val BackgroundDark = Midnight